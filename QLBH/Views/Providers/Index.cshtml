@model IEnumerable<QLBH.Models.Provider>

@{
    ViewBag.Title = "Quản lý nhà cung cấp";
    ViewBag.CardHeader = "Danh sách nhà cung cấp";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        @ViewBag.Title
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li><a href="#">@ViewBag.Title</a></li>
    </ol>
</section>

<!-- Main content -->
<section class="content">
    <p>
        <a href="@Url.Action("Create", "Providers")" class="btn btn-primary"><i class="fa fa-plus"></i>  Tạo mới</a>
    </p>
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">@ViewBag.CardHeader</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    @if (@ViewBag.Noti != null && @ViewBag.Noti.Show == true)
                    {
                        <div class="alert alert-@ViewBag.Noti.Type alert-dismissible">
                            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                            <h4><i class="icon fa fa-ban"></i> Notice!</h4>
                            @ViewBag.Noti.Message
                        </div>
                    }
                    <table class="table table-striped">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Provider_id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Provider_name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Provider_email)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Provider_address)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Provider_phone)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Provider_id)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Provider_name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Provider_email)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Provider_address)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Provider_phone)
                                </td>
                                <td>
                                    @Html.ActionLink("Sửa", "Edit", new { id = item.Provider_id }) |
                                    @Html.ActionLink("Chi tiết", "Details", new { id = item.Provider_id }) |
                                    @Html.ActionLink("Xóa", "Delete", new { id = item.Provider_id })
                                </td>
                            </tr>
                        }

                    </table>
                </div>
                <!-- /.box-body -->
                <div class="box-footer clearfix">

                </div>
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
<!-- /.content -->
